# Anwaltssuche-Scraper

Collects the informations (Full Name, Address, Email, Phone) of all the lawyers using selenium.

## How to install

`pip install scrapy`

`pip install selenium`

## How to Run

`scrapy crawl lawyer`
